project TestProject

define tokens
(
    Int         Int
    +           Add 
    -           Subtract
)

AddOp           ( + | - : type )
Function        ( < Int : left > ( AddOp < Function : right > )? : has_right )
